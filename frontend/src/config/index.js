/**
 * Index file for all configs
 * Centralizes config exports for easy importing
 */

export { default as APP_CONFIG } from './app.config';
export { API_ENDPOINTS } from './api.endpoints';

export default {};
